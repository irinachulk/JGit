{"instr":"# **_Step 10_**\n## Merge with the Master\nTo combine with the wizard, you need to know these commands:\n* ``git -C tmp_git checkout master`` will extract the target branch\n* ``git -C tmp_git merge <new branch>`` will merge commits from branches\n\nA task\n====\nMerge  new branch back to the master."}
